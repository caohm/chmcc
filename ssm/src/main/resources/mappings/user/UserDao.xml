<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.chmcc.springmvc.dao.user.UserDao">
    <resultMap type="org.chmcc.springmvc.model.User" id="User">
        <id column="Id" property="id" jdbcType="BIGINT"></id>
        <result column="organization_id" property="organizationId" jdbcType="BIGINT"></result>
        <result column="username" property="username" jdbcType="VARCHAR"></result>
        <result column="password" property="password" jdbcType="VARCHAR"></result>
        <result column="salt" property="salt" jdbcType="VARCHAR"></result>
        <result column="role_ids" property="roleIds" jdbcType="VARCHAR"></result>
        <result column="locked" property="locked" jdbcType="TINYINT"></result>
    </resultMap>
    <sql id="validFields">
        Id,organization_id,username,password,salt,role_ids,locked
    </sql>

    <select id="getAllUsers" resultType="User">
        select
        <include refid="validFields"/>
        from sys_user
    </select>

    <select id="getUserByUsername" parameterType="string" resultType="User">
        select
        <include refid="validFields"/>
        from sys_user where username=#{username}
    </select>
    <insert id="addUser" parameterType="User">
        insert into sys_user(organization_id,username,password,salt,role_ids,locked)
        values(${organizationId},#{username},#{password},#{salt},#{roleIds},#{locked})
    </insert>

</mapper>